ktor {
    deployment {
        port = 8080
    }
    application {
        modules = [ surik.simyan.locdots.ApplicationKt.module ]
    }
    mongo {
        uri = ${?MONGO_URI}
        database = ${?MONGO_DATABASE}
    }
}